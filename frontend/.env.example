# Opsional. Kosongkan jika memakai rewrite /api di vercel.json (disarankan).
# Isi hanya jika frontend memanggil backend lintas-domain secara langsung:
# VITE_API_URL=https://sicuti-api.vercel.app/api
